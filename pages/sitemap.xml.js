const urls=['/','/products','/contact'];export default function handler(req,res){const base='https://r2-online-shop.vercel.app';const body='<?xml version="1.0" encoding="UTF-8"?>'+'<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+urls.map(path=>`<url><loc>${base}${path}</loc></url>`).join('')+'</urlset>';res.setHeader('Content-Type','application/xml; charset=utf-8');res.setHeader('Cache-Control','public, s-maxage=3600, stale-while-revalidate=86400');res.status(200).send(body)}